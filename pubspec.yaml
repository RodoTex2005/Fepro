name: recetias2_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.5
  google_sign_in: ^7.2.0
  firebase_core: 4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0
  firebase_storage: ^13.0.4
  http: ^1.6.0
  flutter_markdown: 0.6.15
  share_plus: ^13.3.0
  path_provider: ^2.1.0
  

  # --- Agregado para el módulo de reconocimiento de ingredientes ---
  image_picker: ^1.1.2
  flutter_dotenv: ^5.2.1
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/robot.jpeg
    - assets/amelia.jpg
    - .env